a. “pip” là trình quản lý gói cho Python, nó là công cụ cho phép bạn cài đặt và quản lý các thư viện bổ sung và các phần phụ thuộc không được phân phối như các phần có trong thư viện chuẩn.
b. Lệnh “pip freeze” cũng sẽ hiển thị tất cả các gói thư viện đã được cài đặt, nhưng kết quả trả về sẽ được trình bày sao cho phù hợp để làm đầu vào cho lệnh “pip install”.
c. Công cụ có chức năng tương tự “pip” là: apt.
