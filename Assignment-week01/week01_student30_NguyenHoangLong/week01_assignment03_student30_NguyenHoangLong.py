print("a) pip cho phép cài đặt và quản lý các thư viện bổ sung và các phần phụ thuộc không được phân phối như một phần của thư viện chuẩn")
print("b) Lệnh pip freeze cũng sẽ hiển thị tất cả các gói thư viện đã được cài đặt, nhưng kết quả trả về sẽ được trình bày sao cho phù hợp để làm đầu vào cho lệnh pip install. Thông thường kết quả này sẽ được ghi ra file requirements.txt")
print("c) Conda")